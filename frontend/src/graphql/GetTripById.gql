query getTripById($id: String!) {
  getTripById(id: $id) {
    id
    date
    price
    status
    startLocation
    stopLocations
    endLocation
    driver {
      id
      email
    }
    createdAt
    updatedAt
    passengers {
      id
      email
    }
  }
}
